CONFIG = {
    'mode': 'wsgi',
    'working_dir': '/home/{{ user }}/CTFd',
      'python': '/usr/bin/python',
    'user': '{{ user }}',
    'group': '{{ user }}',
    'args': (
        '--bind=127.0.0.1:8000',
        '--workers=1',
        '--umask=0027',
        '--log-level=info',
        '--access-logfile=/var/log/gunicorn/ctfd_access.log',
        'CTFd:create_app()',
    ),
}
